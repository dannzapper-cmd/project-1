# LeadForge Backend — Phase 5.5B dependencies
# Minimal by design. No RAG/vector DB/agent framework — only the official
# provider SDK needed for the Phase 5.5B Groq foundation has been added.

fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
python-dotenv==1.0.1
# Required by FastAPI to parse multipart/form-data (CSV file upload, Fase 4.3B.1).
python-multipart==0.0.20
# Block 10F-A intake file extraction. openpyxl reads .xlsx in memory; pdfplumber
# extracts text-based PDF tables only (no OCR, no system binaries).
openpyxl==3.1.5
pdfplumber==0.11.9
# Official Groq Python SDK — used by GroqModelService (Phase 5.5B). Pulls in
# httpx transitively; we never import httpx directly.
groq==1.2.0

# Dev / test
httpx==0.28.1
pytest==8.3.4
